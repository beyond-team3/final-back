# REMODELING_PIPELINE

이 문서는 영업 파이프라인(`Deal`, `RFQ`, `QUO`, `CNT`, `ORD`, `STMT`, `INV`, `PAY`) 리모델링 전에
반드시 확정해야 하는 정책 결정을 정리하기 위한 체크리스트다.

목표는 아래 두 가지다.

1. 현재 구현의 암묵적 가정을 정책으로 드러낸다.
2. 다중 `deal` / 다중 문서 모델 전환 시 필요한 의사결정을 빠짐없이 확정한다.

이 문서의 각 항목은 가능한 한 다음 형식으로 확정한다.

- 결정: `<최종 정책>`
- 이유: `<왜 이렇게 정하는지>`
- 영향 범위: `<영향받는 도메인/서비스/API>`
- 미결 이슈: `<추가 논의가 필요한 점>`

---

## 1. Deal 정의

### 1-1. Deal의 비즈니스 단위
- 질문: `Deal`은 무엇을 나타내는가?
- 후보:
  - 하나의 거래처와의 단일 영업 기회
  - 하나의 거래처 안의 특정 품목/제안 단위
  - 하나의 계약 체결 시도 단위
  - 하나의 RFQ에서 시작된 전체 문서 체인 단위
- 결정:
- 이유:
- 영향 범위:

### 1-2. 같은 Client의 동시 다중 Deal 허용 여부
- 질문: 같은 `client`에 대해 동시에 열린 `deal`을 여러 개 가질 수 있는가?
- 후보:
  - 허용하지 않음
  - 허용함
- 허용 시 추가 결정:
  - 서로 다른 `deal`을 구분하는 기준은 무엇인가?
  - 사용자가 새 문서를 만들 때 어떤 `deal`에 연결할지 어떻게 결정하는가?
- 결정:
- 이유:
- 영향 범위:

### 1-3. Deal 생성 주체
- 질문: 어떤 액션이 새 `deal`을 생성할 수 있는가?
- 후보:
  - RFQ 생성만 가능
  - RFQ/QUO/CNT 모두 가능
  - 명시적 "새 deal 생성" 액션만 가능
  - 상위 문서 없이 시작 가능한 문서 유형만 가능
- 결정:
- 이유:
- 영향 범위:

### 1-4. Deal 종료 의미
- 질문: `closedAt`이 의미하는 바는 무엇인가?
- 후보:
  - 모든 영업 문서 흐름 종료
  - 더 이상 대표 문서가 없는 상태
  - 결제 및 계약 라이프사이클까지 완전히 종료된 상태
  - 영업 추적 관점에서 종결 처리된 상태
- 결정:
- 이유:
- 영향 범위:

---

## 2. 문서 연결 규칙

### 2-1. 문서가 Deal에 연결되는 기준
- 질문: 새 문서를 만들 때 `deal_id`는 어떻게 결정하는가?
- 후보:
  - 상위 문서의 `deal`을 항상 계승
  - `dealId`를 API 파라미터로 직접 받음
  - 같은 `client`의 최근 열린 `deal` 자동 연결
  - 없으면 새 `deal` 생성, 있으면 선택 UI 제공
- 결정:
- 이유:
- 영향 범위:

### 2-2. 상위 문서 없는 문서 생성 허용 범위
- 질문: QUO/CNT를 상위 문서 없이 직접 생성할 수 있는가?
- 후보:
  - 모두 불가
  - QUO만 가능
  - CNT만 가능
  - 둘 다 가능
- 결정:
- 이유:
- 영향 범위:

### 2-3. 문서 재작성 시 Deal 연결 규칙
- 질문: 반려 후 재작성은 기존 `deal`에 붙는가, 새 `deal`로 분리되는가?
- 후보:
  - 기존 `deal` 유지
  - 새 `deal` 생성
  - 정책에 따라 선택
- 결정:
- 이유:
- 영향 범위:

---

## 3. 단계별 문서 개수 정책

### 3-1. RFQ 개수 정책
- 질문: 하나의 `deal`에 RFQ는 몇 개까지 허용하는가?
- 후보:
  - 1개만 허용
  - 여러 개 허용
- 결정:
- 이유:
- 영향 범위:

### 3-2. QUO 개수 정책
- 질문: 하나의 `deal`에 QUO는 몇 개까지 허용하는가?
- 후보:
  - 진행 중 QUO 1개만 허용
  - 승인 대기/반려/만료 포함 다중 허용
  - 같은 RFQ 기준으로만 1개 제한
  - 같은 `deal` 내 다중 제안 허용
- 결정:
- 이유:
- 영향 범위:

### 3-3. CNT 개수 정책
- 질문: 하나의 `deal` 또는 하나의 `quotation`에 CNT는 몇 개까지 허용하는가?
- 후보:
  - 1개만 허용
  - 반려 시 재작성용 새 문서 허용
  - 병렬 제안까지 허용
- 결정:
- 이유:
- 영향 범위:

### 3-4. ORD 이후 문서 개수 정책
- 질문: `ORD`, `STMT`, `INV`, `PAY`는 `deal`당 여러 개 허용하는가?
- 현재 참고:
  - `ORD`: 한 계약에 여러 개 가능
  - `STMT`: 주문당 1개
  - `INV`: 계약당 활성(DRAFT/PUBLISHED) 1개 수준 제약
  - `PAY`: invoice당 1개
- 결정:
- 이유:
- 영향 범위:

---

## 4. 재작성 정책

### 4-1. 재작성 방식
- 질문: 반려 후 재작성은 상태 변경인가, 새 문서 생성인가?
- 후보:
  - 기존 문서 상태 전이
  - 새 문서 생성
- 권장 검토:
  - 승인 이력 보존
  - 반려 사유 보존
  - 원본/재작성본 추적성
- 결정:
- 이유:
- 영향 범위:

### 4-2. 재작성 가능 조건
- 질문: 어떤 상태의 문서가 재작성 대상이 되는가?
- 후보:
  - `REJECTED_ADMIN`, `REJECTED_CLIENT`만
  - `EXPIRED` 포함
  - step별로 다르게 적용
- 결정:
- 이유:
- 영향 범위:

### 4-3. 재작성 문서의 계보 추적 방식
- 질문: 원본과 재작성본 관계를 어떻게 저장할 것인가?
- 후보:
  - `sourceDocumentId`
  - `originDocumentId`
  - `revisionGroupKey`
  - 별도 연결 테이블
- 결정:
- 이유:
- 영향 범위:

### 4-4. 재작성 가능 목록 조회 기준
- 질문: 목록에는 어떤 문서를 보여주는가?
- 후보:
  - 반려 문서 전부
  - 같은 계보에서 최신 반려 문서만
  - 현재 진행 중 문서가 없는 경우만
- 결정:
- 이유:
- 영향 범위:

---

## 5. 대표 상태(Snapshot) 정책

### 5-1. Deal 대표 문서 선정 규칙
- 질문: `deal.currentStage/currentStatus/latestDocType/latestRefId`는 어떤 문서를 대표하는가?
- 후보:
  - 마지막 이벤트 문서
  - 문서 타입 우선순위 + 최신 문서
  - 현재 진행 중인 최신 문서
  - 상태 우선순위 + 최신 문서
- 결정:
- 이유:
- 영향 범위:

### 5-2. 같은 Step 문서 여러 개일 때 대표 문서 기준
- 질문: 같은 step에 문서가 여러 개면 어떤 문서를 대표로 보는가?
- 후보:
  - 가장 최신 생성 문서
  - 가장 최신 수정 문서
  - 가장 높은 상태 우선순위 문서
  - 상태 우선순위 후 최신 문서
- 결정:
- 이유:
- 영향 범위:

### 5-3. Step 우선순위
- 질문: 서로 다른 step 문서가 공존할 때 우선순위는?
- 후보:
  - `CNT > QUO > RFQ`
  - `ORD > CNT > QUO > RFQ`
  - billing 문서까지 대표 상태에 포함
- 결정:
- 이유:
- 영향 범위:

### 5-4. 삭제/반려/만료 후 snapshot 재계산 규칙
- 질문: 특정 문서가 종료 상태가 되면 snapshot은 어떻게 바뀌는가?
- 후보:
  - 같은 step 최신 문서로 교체
  - 상위/하위 step 전체 재계산
  - 남은 문서가 없으면 중립 상태로 초기화
- 결정:
- 이유:
- 영향 범위:

---

## 6. Deal 종료 조건

### 6-1. 종료 트리거
- 질문: 어떤 조건에서 `deal.close()`를 호출하는가?
- 후보:
  - RFQ 승인 전 삭제
  - QUO 만료
  - CNT 만료
  - 결제 완료 + 계약 만료
  - 같은 `deal`의 모든 문서 종료
- 결정:
- 이유:
- 영향 범위:

### 6-2. 종료 판단 단위
- 질문: 종료 여부는 단일 문서 기준인가, `deal` 전체 문서 집합 기준인가?
- 후보:
  - 단일 문서 이벤트 기반
  - `deal` 전체 문서 재계산 기반
- 결정:
- 이유:
- 영향 범위:

### 6-3. 재오픈 정책
- 질문: 닫힌 `deal`을 다시 열 수 있는가?
- 후보:
  - 불가
  - 새 문서 생성 시 자동 reopen
  - 명시적 reopen만 허용
- 결정:
- 이유:
- 영향 범위:

---

## 7. 승인(Approval) 정책

### 7-1. 승인 단위
- 질문: 승인 흐름은 `deal` 기준인가 문서 기준인가?
- 후보:
  - 문서별 독립 승인
  - step별 단일 승인
  - deal 내 대표 문서만 승인
- 결정:
- 이유:
- 영향 범위:

### 7-2. 동시 승인 허용 여부
- 질문: 같은 `deal`에 여러 승인 대기 문서가 동시에 존재할 수 있는가?
- 후보:
  - 불가
  - QUO만 허용
  - CNT만 허용
  - 모두 허용
- 결정:
- 이유:
- 영향 범위:

### 7-3. 반려 문서와 재작성 문서의 승인 이력 관계
- 질문: 원본 반려 이력과 재작성 승인 요청은 어떻게 구분하는가?
- 결정:
- 이유:
- 영향 범위:

---

## 8. 조회 API 정책

### 8-1. 메인 목록 기준
- 질문: 메인 목록은 `deal` 중심인가 문서 중심인가?
- 후보:
  - `deal` 목록
  - 문서 목록
  - 둘 다 제공
- 결정:
- 이유:
- 영향 범위:

### 8-2. 문서 목록 기본값
- 질문: 기본 목록에 과거 반려/만료 문서를 포함할 것인가?
- 후보:
  - 대표 문서만
  - 전체 문서
  - 옵션으로 분리
- 결정:
- 이유:
- 영향 범위:

### 8-3. Deal 상세 화면 정책
- 질문: `deal` 상세에는 무엇을 보여주는가?
- 체크 대상:
  - 단계별 문서 목록
  - 원본/재작성 계보
  - approval/log/schedule
- 결정:
- 이유:
- 영향 범위:

### 8-4. 재작성 후보 목록 정책
- 질문: 재작성 시작점 목록은 어떤 문서를 대상으로 노출하는가?
- 결정:
- 이유:
- 영향 범위:

---

## 9. 일정(Schedule) 정책

### 9-1. 일정 생성 단위
- 질문: 일정은 `deal` 단위인가 문서 단위인가?
- 후보:
  - 대표 문서 기준
  - 문서별 일정 모두 생성
  - 일부 문서만 일정 생성
- 결정:
- 이유:
- 영향 범위:

### 9-2. 같은 Deal 내 여러 문서 일정 표시
- 질문: 같은 `deal`에 일정이 여러 개면 전부 노출하는가?
- 후보:
  - 전부 노출
  - 대표 일정만 노출
  - 유형별 정책 분리
- 결정:
- 이유:
- 영향 범위:

### 9-3. 재작성/삭제 시 일정 처리
- 질문: 재작성 또는 삭제 시 기존 일정은 어떻게 처리하는가?
- 후보:
  - 원본 일정 유지
  - 원본 일정 삭제, 새 문서 일정 생성
  - 상태에 따라 가시성만 변경
- 결정:
- 이유:
- 영향 범위:

---

## 10. 알림(Notification) 정책

### 10-1. 알림 문맥
- 질문: 알림은 `deal` 중심인가 문서 중심인가?
- 후보:
  - 문서별 독립 알림
  - 대표 문서 기준 알림
  - 문서 + deal 요약 혼합
- 결정:
- 이유:
- 영향 범위:

### 10-2. 재작성 알림
- 질문: 반려 후 재작성 시 어떤 알림을 보내는가?
- 체크 대상:
  - 원본 반려 알림
  - 재작성본 생성 알림
  - 재승인 요청 알림
- 결정:
- 이유:
- 영향 범위:

### 10-3. 수신자 기준
- 질문: 여러 `deal`이 동시에 열려 있을 때 수신자와 문구는 무엇을 기준으로 결정하는가?
- 결정:
- 이유:
- 영향 범위:

---

## 11. 통계(Statistics) 영향 확인

### 11-1. 매출 통계와 Deal 관계
- 질문: 통계 집계가 `deal` 변경에 직접 영향받는가?
- 체크 대상:
  - `Invoice.status = PAID` 기준 유지 여부
  - 재작성 문서가 매출 중복 집계에 영향 없는지
- 결정:
- 이유:
- 영향 범위:

### 11-2. Deal 기반 보조 지표 필요 여부
- 질문: 향후 `deal` 단위 성과 지표를 도입할 것인가?
- 후보:
  - 필요 없음
  - 필요함 (`deal 수`, `성공률`, `deal별 문서 수` 등)
- 결정:
- 이유:
- 영향 범위:

---

## 12. 데이터 모델 추가 결정사항

아래는 리모델링 시 추가로 반드시 결정해야 할 가능성이 높은 항목들이다.

### 12-1. Deal 식별 보조 키 필요 여부
- 질문: 사람이 구분할 수 있는 `dealCode` 또는 제목이 필요한가?
- 이유:
  - 같은 client에 여러 `deal`이 열리면 UI/운영에서 식별이 어려움
- 결정:

### 12-2. Deal 생성 입력값
- 질문: 새 `deal` 생성 시 어떤 메타데이터를 받을 것인가?
- 후보:
  - 제목
  - 품목군
  - 메모
  - 시작 사유 / 유입 경로
- 결정:

### 12-3. 재작성 계보 필드
- 질문: QUO/CNT에 원본 문서 참조 필드를 추가할 것인가?
- 후보:
  - `sourceQuotationId`
  - `sourceContractId`
  - `revisionGroupKey`
- 결정:

### 12-4. 최신 문서 판정 기준 필드
- 질문: "최신" 기준은 무엇인가?
- 후보:
  - `id`
  - `createdAt`
  - `updatedAt`
- 결정:

### 12-5. 마이그레이션 정책
- 질문: 기존 데이터의 `deal`은 새 정책으로 어떻게 해석할 것인가?
- 후보:
  - 기존 데이터 유지, 신규 데이터만 새 정책 적용
  - 배치로 `deal` 재구성
  - 수동 보정 대상 분리
- 결정:

---

## 13. 우선 확정이 필요한 최소 정책

아래 항목이 먼저 확정되지 않으면 구현을 시작하면 안 된다.

1. `Deal`의 비즈니스 단위
2. 같은 `client`의 동시 다중 `deal` 허용 여부
3. `deal` 생성 주체
4. QUO/CNT 재작성 방식
5. 대표 snapshot 선정 규칙
6. `deal` 종료 조건
7. 메인 목록이 `deal` 중심인지 문서 중심인지

---

## 14. 권장 작업 순서

1. `Deal 정의`와 `문서 연결 규칙` 확정
2. `단계별 문서 개수 정책`과 `재작성 정책` 확정
3. `대표 상태`와 `종료 조건` 확정
4. `승인 / 일정 / 알림` 후속 정책 확정
5. 마지막으로 `조회 API`와 `통계 영향` 확정

